### Node.js API + Express.js + Serverless framework + AWS-SDK + DynamoDB + Lambda + DynamoDB

### Requisitos

# Instalando as dependências

$ npm install

# Fazendo o deploy

$ npx serverless deploy
